[build]
publish = "dist"

[functions]
directory = "netlify/functions"
[[plugins]]
package = "netlify-plugin-submit-sitemap"

[plugins.inputs]
baseUrl = "https://nesthomessa.com.au"
sitemapPath = "/sitemap.xml"  # Your manual sitemap location
providers = ["google", "yandex"]
